<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oscarfernandez.pmdmrec02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Usuario"
            android:label="Usuario">
        </activity>
        <activity
            android:name=".VerFase"
            android:label="Ver Fase">
        </activity>
        <activity
            android:name=".Administrador"
            android:label="Administrador">
        </activity>
        <activity
            android:name=".EngadirFase"
            android:label="Engadir Fase">
        </activity>
        <activity
            android:name=".SeleccionarFase"
            android:label="Seleccionar Fase">
        </activity>
    </application>

</manifest>